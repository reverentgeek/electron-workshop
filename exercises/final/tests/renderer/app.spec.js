const chai = require( "chai" );
chai.should();
const electron = require( "electron" );

// Modules for the Renderer process: http://electron.atom.io/docs/v0.37.5/#modules-for-the-renderer-process-web-page

describe( "app.js tests", function() {
	it( "should test renderer", function() {
		console.log( "currently not anything here" );
	} );
} );
